{
  "name": "rollup-plugin-skypack",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "private": false,
  "engines": {
    "node": ">=10"
  },
  "files": [
    "dist/*",
    "src/*"
  ],
  "scripts": {
    "build": "rm -rf dist/ && tsc -d",
    "prepare": "yarn run build"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "find-up": "^5.0.0"
  },
  "peerDependencies": {
    "rollup": "^2"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.2",
    "@types/node": "^14.11.2",
    "fs-extra": "^9.0.1",
    "rollup": "^2.28.2",
    "type-fest": "^0.18.0",
    "typescript": "^4.0.3"
  }
}
